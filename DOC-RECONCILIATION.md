# DOC-RECONCILIATION

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. No code was changed and no builds or tests were executed. It exists so anyone who finds this
later can see exactly what was checked and what was changed.

---

## Per-claim findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `src/app/page.tsx`, `src/features/decisions/actions.ts`, `src/lib/analysis/pipeline.ts`, `README.md:1-14`
The README accurately describes Decision Stress Test as a local-first decision workbench that turns
one decision into a normalized frame, pre-mortem, regret analysis, directional recommendation, and
a Markdown decision memo. The pipeline (`pipeline.ts`) confirms all four analysis stages
(normalization → premortem → regret → synthesis) are implemented and the schema (`schema.ts`)
confirms all storage tables exist.

### 2. Current state

**Status:** `consistent`
**Evidence:** `src/lib/db/schema.ts:15–211`, `src/lib/analysis/pipeline.ts:30–250`,
`tests/` directory (24 test files including unit, integration, e2e, eval)
The code is feature-complete for the v1 workbench. All eight schema tables exist and are populated
by the pipeline. 24 test files are present across unit, integration, e2e, and eval layers. Recent
git history (commits `7a2d016` through `1f5778c`) shows CI and docs improvements, not new feature
work — consistent with a feature-frozen release posture.

### 3. Stack

**Status:** `consistent`
**Evidence:** `package.json:26–60`
README lists: Next.js App Router, TypeScript, Tailwind CSS, Zod, SQLite + Drizzle ORM, Server
Actions, Playwright + Vitest. All confirmed: `next@16.2.3`, `drizzle-orm@0.45.2`,
`better-sqlite3@12.9.0`, `zod@4.3.6`, `tailwindcss@^4`, `vitest@4.1.4`, `@playwright/test@1.59.1`.

### 4. How to run

**Status:** `drifted` (fixed)
**Evidence:** Glob search for `.env*` returned no files; `src/lib/config/env-schema.ts:1–22` shows
all env variables have safe defaults.

README:32 and CLAUDE.md:37 both said:
> `Copy .env.example to .env.local if you want to change defaults.`

No `.env.example` file exists in the repository. All other commands listed in the README
(`npm run dev`, `npm run build`, etc.) are confirmed present in `package.json:5–25`.

**Changes made:**
- `README.md:32` — `.env.example` copy instruction → "Create `.env.local` if you want to override
  defaults. Available variables are declared in `src/lib/config/env-schema.ts`."
- `CLAUDE.md:37` — same fix applied (identical text in the How To Run section)

All remaining setup steps (install, migrate, seed, dev) are confirmed valid.

### 5. Known risks

**Status:** `consistent`
**Evidence:** `docs/adr/0001-local-node-runtime.md`, `src/lib/security/local-access.ts`,
`src/lib/config/env-schema.ts`
README lists: localhost-only, single-user, latest-snapshot-only for edits, read-only history,
Markdown export only. All confirmed by the ADRs, security enforcement code, and schema design.

### 6. Next move

**Status:** `unverifiable`
**Evidence:** forward-looking content
The CLAUDE.md "Next Recommended Move" and README sections on release readiness reference planned
operator actions (SQLite deployment decision, backup verification) that cannot be confirmed or
denied by reading the code alone. Marked unverifiable; text left unchanged.

---

## Contradictions for manual review

### `docs/PORTFOLIO-DISPOSITION.md` — stale commit inventory

`docs/PORTFOLIO-DISPOSITION.md:27–29` states:
> "Only 2 commits: `cbc31d7` feat(decisions): ship local decision workbench / `1c5509f` Initial
> commit from Create Next App"

And `docs/PORTFOLIO-DISPOSITION.md:165` (Last known reference table) states:
> "`origin/main` tip: `cbc31d7`"

The repository now has 8 commits. HEAD is `7a2d016`. The five additional commits added CI (CodeQL),
Dependabot, a security policy, and portfolio context — no new product features.

This document is a historical portfolio disposition snapshot, not a living reference. The
maintainer should decide whether to update the "Last known reference" table and commit count, or
add a note that it reflects the state at `cbc31d7` (the workbench ship commit). The stale data
does not affect how-to-use guidance, only the portfolio inventory view.

**Suggested one-line fix (in the "Last known reference" table):**
Update `origin/main tip` from `cbc31d7` to `7a2d016` and add a note: "Commit inventory
reflects state at workbench ship (`cbc31d7`); 6 subsequent CI/docs commits not listed."

---

## Footer

Run timestamp: 2026-05-30 20:27:33 PDT
Branch: docs/truth-up-2026-05-30
HEAD reconciled against: 7a2d016b8e2fb332730537cd36d6fb1e7716c9ca
